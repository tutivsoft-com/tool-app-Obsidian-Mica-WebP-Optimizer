# Mica public mirror analysis

This mirror reflects the validated Mica surface. Local media conversion, preview, and rollback remain useful without optional setup; billing is account-scoped, authenticated, and fail closed with stable pending events. Media and secrets are excluded from diagnostics. Private-source check/build/publish parity validation precedes mirror synchronization.
